# Dictionary is a collection of keys-value pairs

# d ={} Empty Dict

a = {"key":"value",
     "harry":"code",
     "marks":100,
      list:[1,2,9]
}

print(a["key"]) # prints "value"
print(a[list]) # prints [1,2,9]

# Dicts are mutable and cannot contain duplicate keys 